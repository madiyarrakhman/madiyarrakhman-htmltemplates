# 🚀 Руководство Пользователя и Управление Платформой

Это руководство содержит инструкции по управлению вашей платформой свадебных приглашений и использованию ее API.

[English Version (Английская версия)](PLATFORM_GUIDE.md)

---

## 🔐 Панель Администратора

Визуальный интерфейс для управления всеми приглашениями.
- **URL**: `https://your-domain.com/admin`
- **Логин/Пароль**: Устанавливаются через переменные окружения (`ADMIN_USERNAME` и `ADMIN_PASSWORD`).
- **Функционал**: Создание новых приглашений, просмотр статистики RSVP (ответов гостей) и управление существующими записями.

## 🔐 Ключи Доступа

- **Приватный API Ключ (Private API Key)**: Используется для конфиденциальных операций, таких как создание и редактирование приглашений. Передавайте его в заголовках как `x-api-key`.
- **Публичный Ключ (Frontend Public Key)**: Генерируется автоматически, чтобы фронтенд мог безопасно получать данные приглашения и отправлять ответы гостей.

## 🛠 Справочник API Эндпоинтов

### 1. Создание приглашения
Генерация нового приглашения для клиента.
- **Метод**: `POST`
- **Путь**: `/api/invitations`
- **Аутентификация**: `x-api-key: ВАШ_ПРИВАТНЫЙ_КЛЮЧ`
- **Пример тела запроса**:
```json
{
  "phoneNumber": "+77011234567",
  "templateCode": "silk-ivory",
  "lang": "ru",
  "groomName": "Арман",
  "brideName": "Айгерим",
  "eventDate": "2026-08-15 17:00:00",
  "eventLocation": "Rixos, Алматы",
  "content": "Ждем вас на нашем празднике!"
}
```

### 2. Получение данных приглашения
Используется фронтендом для отображения данных по конкретной ссылке гостя.
- **Метод**: `GET`
- **Путь**: `/api/invitations/:uuid`

### 3. Отправка RSVP
Ответ гостя на приглашение.
- **Метод**: `POST`
- **Путь**: `/api/rsvp/:uuid`
- **Пример данных**:
```json
{
  "guestName": "Иван Иванов",
  "attendance": "yes",
  "guestCount": 2
}
```

## 🤖 Автоматизация через n8n / Zapier

Платформа создана для бесшовной автоматизации:
1. Используйте ноду **HTTP Request**.
2. Отправьте `POST` запрос на `/api/invitations` с вашим API-ключом.
3. Используйте возвращенный `fullUrl` для автоматической рассылки приглашений через WhatsApp, СМС или Email.

## 🌍 Жизненный цикл процесса

1. **Генерация**: Создайте приглашение (через Панель админа или API).
2. **Рассылка**: Поделитесь уникальной ссылкой `domain.com/i/UUID` с гостями.
3. **Просмотр**: Гости открывают персонализированный интерактивный сайт.
4. **RSVP**: Гости отправляют статус своего присутствия.
5. **Мониторинг**: Отслеживайте все ответы в реальном времени в Панели администратора.

## 🗄 Архитектура Базы Данных

- **`templates`**: Справочник доступных дизайнов интерфейса.
- **`invitations`**: Хранит основные данные, персонализированный контент и UUID.
- **`rsvp_responses`**: Связаны с приглашениями, хранят имена гостей и их количество.

---
По техническим вопросам, пожалуйста, обращайтесь к `README.md` или `api/README.md`.
